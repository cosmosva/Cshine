<!--会议纪要列表页 - 知识库 v2-->
<wxs module="utils" src="./list.wxs"></wxs>
<view class="meeting-list-page">
  <!-- 品牌栏（第一层导航） -->
  <view class="brand-bar">
    <view class="brand-left">
      <view class="logo-circle">C</view>
      <text class="brand-name">Cshine</text>
      <view class="record-btn" bindtap="goToRecord">
        <text class="record-icon">●</text>
        <text class="record-text">录音</text>
      </view>
    </view>
    <view class="brand-right">
    </view>
  </view>

  <!-- 二级导航栏 -->
  <view class="header">
    <view class="header-left">
      <image class="menu-icon" src="/assets/icons/menu.png" mode="aspectFit" bindtap="toggleDrawer"></image>
      <text class="title">{{currentFolderName}}</text>
    </view>
    <view class="header-right">
      <image class="icon-btn search-icon" src="/assets/icons/search.png" mode="aspectFit"></image>
      <image class="icon-btn add-icon" src="/assets/icons/add.png" mode="aspectFit" bindtap="showUploadMenu"></image>
      <view class="sort-btn" bindtap="showSortMenu">
        <text class="icon">⇅</text>
      </view>
    </view>
  </view>

  <!-- 主内容区 -->
  <view class="page-content">
    <!-- 会议列表 -->
    <scroll-view 
      class="meeting-scroll"
      scroll-y
      enhanced
      show-scrollbar="{{false}}"
      enable-back-to-top
      refresher-enabled
      refresher-triggered="{{refreshing}}"
      bindrefresherrefresh="onPullDownRefresh"
      bindscrolltolower="onReachBottom"
    >
      <view class="meeting-list">
        <view 
          class="meeting-card"
          wx:for="{{meetingList}}"
          wx:key="id"
          bindtap="viewMeeting"
          bindlongpress="onMeetingLongPress"
          data-id="{{item.id}}"
          data-title="{{item.title}}"
          data-status="{{item.status}}"
          data-folder-id="{{item.folder_id}}"
          data-folder-name="{{item.folder_name}}"
        >
          <!-- 第一行：标题 + 收藏 -->
          <view class="card-row-1">
            <text class="title">{{item.title}}</text>
            <view class="star-btn" catchtap="toggleFavorite" data-id="{{item.id}}" data-favorite="{{item.is_favorite}}">
              <text class="star-icon">{{item.is_favorite ? '⭐' : '☆'}}</text>
            </view>
          </view>

          <!-- 第二行：录音时间 + 录音时长 -->
          <view class="card-row-2">
            <text class="timestamp">{{utils.formatFullTime(item.created_at)}}</text>
            <text class="duration" wx:if="{{item.audio_duration}}">{{utils.formatDuration(item.audio_duration)}}</text>
          </view>

          <!-- 第三行：扬声器图标 + 标签 -->
          <view class="card-row-3">
            <text class="audio-icon">🔊</text>
            <text class="source-label">扬声器</text>
            <view class="tags-container" wx:if="{{item.tags && item.tags.length > 0}}">
              <text class="tag" wx:for="{{item.tags}}" wx:key="index" wx:for-item="tag">{{tag}}</text>
            </view>
          </view>
        </view>

        <!-- 空状态 -->
        <view class="empty-state" wx:if="{{meetingList.length === 0 && !loading}}">
          <text class="empty-icon">📋</text>
          <text class="empty-text">{{emptyText}}</text>
          <text class="empty-hint">点击右上角上传会议音频开始</text>
        </view>

        <!-- 加载提示 -->
        <view class="load-more" wx:if="{{loading && meetingList.length > 0}}">
          <text class="loading-text">加载中...</text>
        </view>

        <view class="load-more" wx:if="{{!loading && !hasMore && meetingList.length > 0}}">
          <text class="no-more-text">没有更多了</text>
        </view>
      </view>
    </scroll-view>
  </view>

  <!-- 筛选菜单（ActionSheet） -->
  <view class="action-sheet-mask" wx:if="{{showFilterSheet}}" bindtap="hideFilterSheet" catchtouchmove="preventDefault"></view>
  <view class="action-sheet {{showFilterSheet ? 'show' : ''}}">
    <view class="action-sheet-header">
      <text class="action-sheet-title">筛选</text>
      <view class="action-sheet-close" bindtap="hideFilterSheet">✕</view>
    </view>
    <view class="action-sheet-body">
      <view 
        class="action-item {{currentFilter === item.value ? 'active' : ''}}"
        wx:for="{{filterOptions}}"
        wx:key="value"
        bindtap="selectFilter"
        data-value="{{item.value}}"
        data-label="{{item.label}}"
      >
        <text class="action-label">{{item.label}}</text>
        <text class="action-check" wx:if="{{currentFilter === item.value}}">✓</text>
      </view>
    </view>
  </view>

  <!-- 排序菜单（ActionSheet） -->
  <view class="action-sheet-mask" wx:if="{{showSortSheet}}" bindtap="hideSortSheet" catchtouchmove="preventDefault"></view>
  <view class="action-sheet {{showSortSheet ? 'show' : ''}}">
    <view class="action-sheet-header">
      <text class="action-sheet-title">排序</text>
      <view class="action-sheet-close" bindtap="hideSortSheet">✕</view>
    </view>
    <view class="action-sheet-body">
      <view 
        class="action-item {{currentSort === item.value ? 'active' : ''}}"
        wx:for="{{sortOptions}}"
        wx:key="value"
        bindtap="selectSort"
        data-value="{{item.value}}"
      >
        <text class="action-label">{{item.label}}</text>
        <text class="action-check" wx:if="{{currentSort === item.value}}">✓</text>
      </view>
    </view>
  </view>

  <!-- 侧边栏抽屉 - 知识库管理 -->
  <view class="drawer-mask {{showDrawer ? 'show' : ''}}" bindtap="closeDrawer" catchtouchmove="preventDefault"></view>
  <view class="drawer {{showDrawer ? 'show' : ''}}">
    <!-- 整个抽屉滚动区域 -->
    <scroll-view class="drawer-scroll" scroll-y enhanced show-scrollbar="{{false}}">
      <!-- 抽屉头部 -->
      <view class="drawer-header">
        <text class="drawer-title">知识库管理</text>
      </view>

      <!-- 抽屉内容 -->
      <view class="drawer-content">
        <!-- 录音文件（全部） -->
        <view class="drawer-item all-files" bindtap="selectAllFiles">
          <view class="item-left">
            <image class="folder-icon" src="/assets/icons/folder.png" mode="aspectFit"></image>
            <text class="item-name">录音文件</text>
            <text class="item-count">({{totalCount}})</text>
          </view>
        </view>

        <!-- 我创建的分组 -->
        <view class="drawer-section">
          <view class="section-header">
            <text class="section-title">我创建的</text>
            <view class="section-actions">
              <image class="action-icon" src="/assets/icons/add-folder.png" mode="aspectFit" bindtap="handleCreateFolder"></image>
              <image class="action-icon" src="/assets/icons/sort.png" mode="aspectFit"></image>
            </view>
          </view>

          <!-- 文件夹列表 -->
          <view
            class="drawer-item folder-item"
            wx:for="{{folders}}"
            wx:key="id"
            bindtap="selectFolder"
            data-id="{{item.id}}"
          >
            <view class="item-left">
              <image class="folder-icon" src="/assets/icons/folder.png" mode="aspectFit"></image>
              <text class="item-name">{{item.name}}</text>
              <text class="item-count">({{item.count}})</text>
            </view>
            <text class="more-icon" catchtap="showFolderMenu" data-id="{{item.id}}" data-name="{{item.name}}">⋯</text>
          </view>
        </view>
      </view>
    </scroll-view>
  </view>

  <!-- 上传操作选择 Modal（TicNote 风格） -->
  <view class="modal-mask {{showUploadSheet ? 'show' : ''}}" wx:if="{{showUploadSheet}}" bindtap="hideUploadSheet" catchtouchmove="preventDefault"></view>
  <view class="upload-modal modal {{showUploadSheet ? 'show' : ''}}" wx:if="{{showUploadSheet}}" catchtap="preventClose">
    <view class="modal-header center">
      <text class="modal-title">上传与新建</text>
      <view class="modal-close" bindtap="hideUploadSheet">✕</view>
    </view>
    <view class="modal-body no-padding">
      <view class="upload-option-item" catchtap="handleUploadFile">
        <image class="option-icon" src="/assets/icons/upfile.png" mode="aspectFit"></image>
        <text class="option-label">上传文件</text>
      </view>
      <view class="upload-option-item" catchtap="handleCreateFolder">
        <image class="option-icon" src="/assets/icons/add-folder.png" mode="aspectFit"></image>
        <text class="option-label">新建知识库</text>
      </view>
    </view>
  </view>

  <!-- 知识库选择 Modal -->
  <view class="modal-mask" wx:if="{{showFolderSelector}}" bindtap="hideFolderSelector" catchtouchmove="preventDefault"></view>
  <view class="modal folder-selector {{showFolderSelector ? 'show' : ''}}" catchtap="preventClose">
    <view class="modal-header">
      <text class="modal-title">选择知识库</text>
    </view>
    <view class="modal-body">
      <text class="current-folder-label">当前知识库：{{currentFolderName}}</text>

      <scroll-view class="folder-list" scroll-y>
        <!-- 录音文件（全部） -->
        <view class="folder-option {{currentFolderId === null ? 'selected' : ''}}"
              bindtap="selectFolderForUpload" data-id="{{null}}" data-name="录音文件">
          <text class="folder-name">录音文件</text>
          <text class="check-icon" wx:if="{{currentFolderId === null}}">✓</text>
        </view>

        <!-- 用户创建的知识库 -->
        <view class="folder-option {{currentFolderId === item.id ? 'selected' : ''}}"
              wx:for="{{folders}}" wx:key="id"
              bindtap="selectFolderForUpload" data-id="{{item.id}}" data-name="{{item.name}}">
          <text class="folder-name">{{item.name}}</text>
          <text class="check-icon" wx:if="{{currentFolderId === item.id}}">✓</text>
        </view>
      </scroll-view>

      <view class="modal-actions">
        <view class="btn-secondary" bindtap="handleCreateFolderFromSelector">新建知识库</view>
        <view class="btn-primary" bindtap="confirmFolderSelection">确认</view>
      </view>
    </view>
  </view>

  <!-- 知识库操作菜单 ActionSheet -->
  <view class="action-sheet-mask" wx:if="{{showFolderActions}}" bindtap="hideFolderActions" catchtouchmove="preventDefault"></view>
  <view class="action-sheet {{showFolderActions ? 'show' : ''}}">
    <view class="action-sheet-header">
      <text class="action-sheet-title">{{selectedFolderName}}</text>
      <view class="action-sheet-close" bindtap="hideFolderActions">✕</view>
    </view>
    <view class="action-sheet-body">
      <view class="action-item" bindtap="handleRenameFolder">
        <text class="action-icon">✏️</text>
        <text class="action-label">重命名</text>
      </view>
      <view class="action-item danger" bindtap="handleDeleteFolder">
        <text class="action-icon">🗑️</text>
        <text class="action-label">删除</text>
      </view>
    </view>
  </view>

  <!-- 知识库选择器 Modal（用于上传文件） -->
  <view class="modal-mask" wx:if="{{showUploadFolderSelector}}" bindtap="cancelUploadFolderSelection" catchtouchmove="preventDefault"></view>
  <view class="folder-selector-modal {{showUploadFolderSelector ? 'show' : ''}}" catchtap="preventClose">
    <view class="modal-header">
      <text class="modal-title">选择知识库</text>
    </view>
    <view class="modal-body">
      <scroll-view class="folder-list" scroll-y>
        <!-- 录音文件（未分类） -->
        <view class="folder-option {{uploadTargetFolderId === null ? 'selected' : ''}}"
              bindtap="selectUploadTargetFolder" data-id="{{null}}">
          <text class="folder-name">📁 录音文件（未分类）</text>
          <text class="check-icon" wx:if="{{uploadTargetFolderId === null}}">✓</text>
        </view>

        <!-- 用户创建的知识库 -->
        <view class="folder-option {{uploadTargetFolderId === item.id ? 'selected' : ''}}"
              wx:for="{{folders}}" wx:key="id"
              bindtap="selectUploadTargetFolder" data-id="{{item.id}}">
          <text class="folder-name">📁 {{item.name}}</text>
          <text class="check-icon" wx:if="{{uploadTargetFolderId === item.id}}">✓</text>
        </view>
      </scroll-view>

      <view class="modal-actions">
        <view class="btn-secondary" bindtap="cancelUploadFolderSelection">取消</view>
        <view class="btn-primary" bindtap="confirmFolderSelection">确认</view>
      </view>
    </view>
  </view>

  <!-- 知识库选择器 Modal（用于复制/移动会议） -->
  <view class="modal-mask" wx:if="{{showFolderSelector}}" bindtap="hideFolderSelectorModal" catchtouchmove="preventDefault"></view>
  <view class="folder-selector-modal {{showFolderSelector ? 'show' : ''}}" catchtap="preventClose">
    <view class="modal-header">
      <text class="modal-title">{{folderSelectorAction === 'copy' ? '复制到' : '移动到'}}</text>
    </view>
    <view class="modal-body">
      <text class="current-folder-label">当前位置：{{meetingCurrentFolderName}}</text>

      <scroll-view class="folder-list" scroll-y>
        <!-- 录音文件（未分类） -->
        <view class="folder-option {{selectedTargetFolderId === null ? 'selected' : ''}}"
              bindtap="selectTargetFolder" data-id="{{null}}">
          <text class="folder-name">📁 录音文件（未分类）</text>
          <text class="check-icon" wx:if="{{selectedTargetFolderId === null}}">✓</text>
        </view>

        <!-- 用户创建的知识库 -->
        <view class="folder-option {{selectedTargetFolderId === item.id ? 'selected' : ''}}"
              wx:for="{{folders}}" wx:key="id"
              bindtap="selectTargetFolder" data-id="{{item.id}}">
          <text class="folder-name">📁 {{item.name}}</text>
          <text class="check-icon" wx:if="{{selectedTargetFolderId === item.id}}">✓</text>
        </view>
      </scroll-view>

      <view class="modal-actions">
        <view class="btn-secondary" bindtap="hideFolderSelectorModal">取消</view>
        <view class="btn-primary" bindtap="confirmFolderAction">确认</view>
      </view>
    </view>
  </view>
</view>
